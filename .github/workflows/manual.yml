name: VPS Auto Renew

on:
  schedule:
    # 北京时间每天上午 9 点运行 (UTC时间 1:00)
    # 你可以根据需要调整 cron 表达式
    - cron: '0 1 * * *'
  workflow_dispatch: # 允许手动点按钮运行

jobs:
  renew:
    runs-on: ubuntu-latest
    
    # 设置超时防止卡死
    timeout-minutes: 15

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        # 确保 pip 升级，并在当前环境中安装依赖
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Install Playwright Browsers
      run: |
        # 显式使用 python -m 调用 playwright，确保找到命令
        python -m playwright install chromium
        python -m playwright install-deps

    - name: Run Auto Renew Script
      env:
        # 这里映射你在 GitHub Secrets 中填写的变量
        URL: ${{ secrets.URL }}
        GMAIL: ${{ secrets.GMAIL }}
        KATAMIMA: ${{ secrets.KATAMIMA }}
        TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
        TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        AUTH_JSON: ${{ secrets.AUTH_JSON }}
      run: |
        # 脚本运行
        python main.py

    - name: Upload Videos, Screenshots & Auth File
      # 无论成功失败，都上传录屏和截图供检查
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: run-evidence
        path: |
          videos/
          *.png
          auth.json
        retention-days: 7
