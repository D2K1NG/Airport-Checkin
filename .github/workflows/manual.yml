name: VPS Auto Renew

on:
  schedule:
    # 北京时间每天早上 8:00 (UTC 0:00)
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许手动触发

jobs:
  renew:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        pip install requests playwright
        playwright install chromium

    - name: Run Renew Script
      env:
        GMAIL: ${{ secrets.GMAIL }}
        KATAMIMA: ${{ secrets.KATAMIMA }}
      run: python main.py

    # 👇👇👇 修改了这里，增加了视频文件的上传 👇👇👇
    - name: Upload Debug Artifacts (Screenshots & Video)
      if: always() # 无论成功失败都上传，方便排查问题
      uses: actions/upload-artifact@v4
      with:
        name: debug-artifacts # Artifact 的名称
        path: |
          *.png
          videos/*.webm
        retention-days: 3 # 文件保留3天
